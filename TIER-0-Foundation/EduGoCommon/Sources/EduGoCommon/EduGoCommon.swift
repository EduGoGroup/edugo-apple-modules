/// EduGoCommon - Foundation module
///
/// Provides common utilities, extensions, and base types used across all modules.
/// This is the foundational tier (TIER-0) with zero dependencies on other EduGo modules.
public struct EduGoCommon {
    /// Module version
    public static let version = "1.0.0"

    public init() {}
}
